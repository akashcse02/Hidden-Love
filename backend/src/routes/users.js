import{Router}from"express";import multer from"multer";import{auth}from"../middleware/auth.js";import{me,search,follow,avatar}from"../controllers/users.js";const r=Router(),u=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024}});r.get("/me",auth,me);r.get("/search",auth,search);r.post("/:id/follow",auth,follow);r.post("/avatar",auth,u.single("avatar"),avatar);export default r;
